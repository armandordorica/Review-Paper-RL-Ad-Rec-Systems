# Round-2 DOI checklist

Interactive UI: [`doi_checklist.html`](doi_checklist.html)

Data snapshot: [`doi_checklist_data.json`](doi_checklist_data.json)

Open in a browser:

```bash
open docs/agent/doi_checklist.html
```

Agent first-pass (Crossref title vs bib title) is embedded. Mark each entry Verified OK / Fix DOI / Remove DOI, then export JSON back to chat for bib edits.
